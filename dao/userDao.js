const User = require('../models/user');

const createUser = (userData) => {
    // Implementación del DAO para crear un usuario
};

const findUserByEmail = (email) => {
    // Implementación del DAO para buscar un usuario por correo electrónico
};

module.exports = {
    createUser,
    findUserByEmail
};
